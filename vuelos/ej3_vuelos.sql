-- Ejercicio 3

-- 0
SELECT AVG(OCUPACION) AS OCUPACION_MEDIA, SUM(OCUPACION) AS TOTAL_PASAJEROS
FROM VUELO
WHERE FECHA = TO_DATE('10-FEB-2018', 'DD-MON-YYYY');

-- 1
SELECT DESTINO, COUNT(*)
FROM RUTA
GROUP BY DESTINO;


-- 2
SELECT r.COMPAN, SUM(r.NUM_HORAS)
FROM VUELO v
JOIN RUTA r ON v.COD = r.COD
GROUP BY r.COMPAN;


-- 3





SELECT * FROM VUELO;
SELECT * FROM RUTA;
SELECT * FROM AVION;